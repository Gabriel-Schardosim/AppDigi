export default class AtualizacaoGlobal {

  static atualizar(oldRealm: Realm, newRealm: Realm) {

    // EXEMPLOS
    // if (newRealm.schemaVersion === 1) {
    //   const oldObjects = oldRealm.objects<Bairro>('Bairro');
    //   const newObjects = newRealm.objects<Bairro>('Bairro');
    //
    //   // loop through all objects and set the name property in the new schema
    //   for (let i = 0; i < oldObjects.length; i++) {
    //     newObjects[i].nomeDNE = oldObjects[i].nome + ' ' + (oldObjects[i]?.codigoDNE || ' AAAAAAAAA');
    //   }
    // }
    //
    // if (newRealm.schemaVersion === 2) {
    //
    //
    // }


  }

}
;
